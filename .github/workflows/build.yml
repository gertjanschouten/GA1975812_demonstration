name: build

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Create test file
        run: echo "Hello" > test.txt
      - name: Push test file
        run: |
          git config --global user.name "gertjanschouten"
          git config --global user.email "gertjanschouten@users.noreply.github.com"
          git remote set-url origin https://x-access-token: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@github.com/${{ github.repository }}
          git add -A
          git commit -am "Commit test.txt"
          git push
